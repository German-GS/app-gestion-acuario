rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /aquarium_images/{userId}/{imageId} {
      allow write: if request.auth != null; // Usamos la regla simple para depurar
      allow read;
    }
  }
}